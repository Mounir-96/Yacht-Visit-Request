package org.example;

import org.openqa.selenium.WebDriver;

import java.util.Scanner;

// Press Shift twice to open the Search Everywhere dialog and type `show whitespaces`,
// then press Enter. You can now see whitespace characters in your code.
public class Main {
   public static void main(String[] args) {
        /*Scanner input = new Scanner(System.in);

        int rand=(int)(Math.random()*10);
        boolean Guess=true;
        while(Guess) {
            System.out.println("Guess a Number Between 1 and 10");
            int n = input.nextInt();
            if (n<10&&n==rand){
                System.out.println("EXCELLENT !!! Correct Guess");
                break;
            }
            else if(n>10 || n<0){
                System.out.println("Invalid Number .must be less than 10 and greater than 0");
            }
              else
                  System.out.println("Your Guess is "+n +" and it is not correct ,Please Guess again.");
        }*/

           /* Scanner input = new Scanner(System.in);
            int []arr=new int [5];
            int max=0,min=1000,sum=0;

            for(int i=0;i<arr.length;i++){
                System.out.println("Enter Element number "+(i+1));
                arr[i]=input.nextInt();
                if(arr[i]<min){min=arr[i];}
                else if(arr[i]>max){max=arr[i];}
                sum+=arr[i];


            }
            int maxsum =sum-min;
            int minsum = sum-max;
            System.out.println( max + " "+min);
            System.out.print (+minsum +" " );
            System.out.print (+maxsum );*/




        }
        }

